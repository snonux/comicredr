/// Panel and balloon analysis: the data model, reading order and the
/// confidence gate. Detection itself lands in M4 (classic CV) and M5 (model).
library;

export 'src/gate.dart';
export 'src/panel.dart';
export 'src/reading_order.dart';
