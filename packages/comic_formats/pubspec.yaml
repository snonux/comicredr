name: comic_formats
description: ComicRedr comic formats layer, pure Dart with no Flutter dependency.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.4

dev_dependencies:
  test: ^1.26.0
dependencies:
  archive: ^4.3.0
  crypto: ^3.0.7
  pdfrx_engine: ^0.6.1
