name: comic_sync
description: ComicRedr S3 sync, pure Dart with no Flutter dependency.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.4

dependencies:
  http: ^1.1.0
  minio: ^3.5.8

dev_dependencies:
  lints: ^6.1.0
  test: ^1.26.0
